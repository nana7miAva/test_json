create table mock_z(
id INT primary key NOT NULL AUTO_INCREMENT,
name varchar(255),
phone varchar(255),
university varchar(255),
city varchar(255),
street varchar(255),
salary DOUBLE,
job varchar(255),
load_data TIMESTAMP
) DEFAULT CHARSET=utf8;